# Playwright Mocking Lab

Track 1 — Topic 1: Playwright Mocking (REST first)

DoD: DONE when I can explain it clearly and demo it once without help.

## Focus
- REST API mocking with page.route()/context.route()
- route.fulfill(), route.abort(), route.continue()
